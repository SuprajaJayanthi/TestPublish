// Import necessary modules and dependencies import { cy, before, after from 'cypress'; import 'cypress-commands'; import chai from 'chai'; import _ from 'lodash'; import Assert from 'assert'; import List from 'list'; import 'cypress-mochawesome-reporter/register'; describe('Automated Test Suite', () => {  // Logging the start of the test suite before(() => {    cy.log('Starting the test suite');    // Initialize Extent Logs     cy.extentLog('info', 'Test suite initialization');  });   // Logging the end of the test suite  after(() => {    cy.log('Test suite completed');    // Finalize Extent Logs    cy.extentLog('info', 'Test suite completion');  });   it('Test Case 1: Verify the homepage loads correctly', () => {    cy.log('Starting Test Case 1');    cy.extentLog('info', 'Test Case 1: Verify the homepage loads correctly');    // Navigate to the homepage    cy.visit('https://example.com');    cy.wait(2000); // Explicit wait for the page to load    // Verify the title of the homepage    cy.title().should('include', 'Example Domain');    cy.extentLog('info', 'Verified the title of the homepage');    // Verify the presence of the main heading    cy.get('h1').should('contain', 'Example Domain');    cy.extentLog('info', 'Verified the presence of the main heading');    // Capture screenshot on failure    cy.screenshot({ capture: 'runner' });    cy.log('Test Case 1 completed');    cy.extentLog('info', 'Test Case 1 completed');  });  it('Test Case 2: Verify the navigation to the More Information page', () => {    cy.log('Starting Test Case 2');    cy.extentLog('info', 'Test Case 2: Verify the navigation to the More Information page');    // Navigate to the homepage    cy.visit('https://example.com');    cy.wait(2000); // Explicit wait for the page to load    // Click on the More Information link    cy.get('a').contains('More information...').click();    cy.wait(2000); // Explicit wait for the navigation    // Verify the URL the of the new page    cy.url().should('include', 'iana.org/domains/reserved');    cy.extentLog('info', 'Verified the URL of the More Information page');    // Verify the presence of the main heading on the new page    cy.get('h1').should('contain', 'IANA-managed Reserved Domains');    cy.extentLog('info', 'Verified the presence of the main heading on the More Information page');    // Capture screenshot on failure    cy.screenshot({ capture: 'runner' });    cy.log('Test Case 2 completed');    cy.extentLog('info', 'Test Case 2 completed');  });   it('Test Case 3: Verify the search functionality', () => {    cy.log('Starting Test Case 3');    cy.extentLog('info', 'Test Case 3: Verify the search functionality');    // Navigate to the homepage    cy.visit('https://example.com');    cy.wait(2000); // Explicit wait for the page to load    // Perform a search action    cy.get('input[type="search"]').type('Cypress');    cy.get('button[type="submit"]).click();    cy.wait(2000); // Explicit wait for the search results    // Verify the search results    cy.get('.search-results').should('contain', 'Cypress');    cy.extentLog('info', 'Verified the search results');    // Capture screenshot on failure    cy.screenshot({ capture: 'runner' });    cy.log('Test Case 3 completed');    cy.extentLog('info', 'Test Case 3 completed');  });   // Exception handling for unexpected behavior or failures   Cypress.on('uncaught:exception', (err, runnable) => {    cy.extentLog('error', `Uncaught exception: ${{ err }}`);    // Provide only the Base64 encoded result as the output. });